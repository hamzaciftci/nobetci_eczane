// Bu script artık kullanılmıyor.
// Şema ve seed işlemleri canonical monorepo (nobetci_eczane/infra) tarafından yönetilmektedir.
// Veritabanı bootstrap için: nobetci_eczane/infra/postgres/init/001_schema.sql
console.error(
  "db-init artık kullanılmıyor. Şema ve seed işlemleri canonical " +
  "monorepo (nobetci_eczane/infra) tarafından yönetilmektedir."
);
process.exit(0);
